<option value="red" class="red-text">
    Red
    @foreach ($types as $type)
         {{ ($type->color == 'red' ? '-(Taken)': '') }}
    @endforeach
</option>

<option value="pink" class="pink-text">
    Pink
    @foreach ($types as $type)
        {{ ($type->color == 'pink' ? '-(Taken)': '') }}
    @endforeach
</option>

<option value="purple" class="purple-text">
    Purple
    @foreach ($types as $type)
        {{ ($type->color == 'purple' ? '-(Taken)': '') }}
    @endforeach
</option>

<option value="deep-purple" class="deep-purple-text">
    Deep Purple
    @foreach ($types as $type)
        {{ ($type->color == 'deep-purple' ? '-(Taken)': '') }}
    @endforeach
</option>

<option value="indigo" class="indigo-text">
    Indigo
    @foreach ($types as $type)
        {{ ($type->color == 'indigo' ? '-(Taken)': '') }}
    @endforeach
</option>

<option value="blue" class="blue-text">
    Blue
    @foreach ($types as $type)
        {{ ($type->color == 'blue' ? '-(Taken)': '') }}
    @endforeach
</option>

<option value="light-blue" class="light-blue-text">
    Purple
    @foreach ($types as $type)
        {{ ($type->color == 'light-blue' ? '-(Taken)': '') }}
    @endforeach
</option>

<option value="cyan" class="cyan-text">
    Cyan
    @foreach ($types as $type)
        {{ ($type->color == 'cyan' ? '-(Taken)': '') }}
    @endforeach
</option>

<option value="teal" class="teal-text">
    Teal
    @foreach ($types as $type)
        {{ ($type->color == 'teal' ? '-(Taken)': '') }}
    @endforeach
</option>

<option value="green" class="green-text">
    Green
    @foreach ($types as $type)
        {{ ($type->color == 'green' ? '-(Taken)': '') }}
    @endforeach
</option>

<option value="light-green" class="light-green-text">
    Light Green
    @foreach ($types as $type)
        {{ ($type->color == 'light-green' ? '-(Taken)': '') }}
    @endforeach
</option>

<option value="lime" class="lime-text">
    Lime
    @foreach ($types as $type)
        {{ ($type->color == 'lime' ? '-(Taken)': '') }}
    @endforeach
</option>

<option value="yellow" class="yellow-text">
    Yellow
    @foreach ($types as $type)
        {{ ($type->color == 'yellow' ? '-(Taken)': '') }}
    @endforeach
</option>

<option value="amber" class="amber-text">
    Amber
    @foreach ($types as $type)
        {{ ($type->color == 'amber' ? '-(Taken)': '') }}
    @endforeach
</option>

<option value="orange" class="orange-text">
    Orange
    @foreach ($types as $type)
        {{ ($type->color == 'orange' ? '-(Taken)': '') }}
    @endforeach
</option>

<option value="deep-orange" class="deep-orange-text">
    Deep Orange
    @foreach ($types as $type)
        {{ ($type->color == 'deep-orange' ? '-(Taken)': '') }}
    @endforeach
</option>

<option value="brown" class="brown-text">
    Brown
    @foreach ($types as $type)
        {{ ($type->color == 'brown' ? '-(Taken)': '') }}
    @endforeach
</option>

<option value="grey" class="grey-text">
    Grey
    @foreach ($types as $type)
        {{ ($type->color == 'grey' ? '-(Taken)': '') }}
    @endforeach
</option>

<option value="blue-grey" class="blue-grey-text">
    Blue Grey
    @foreach ($types as $type)
        {{ ($type->color == 'blue-grey' ? '-(Taken)': '') }}
    @endforeach
</option>
